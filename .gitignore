/target
*.swp
*.swo
*~
.DS_Store
.claude-state.json
LIVE_STATE.json
.git-backup/
*.keystore
.claude/
testnet/data/
__pycache__/
*.pyc

# build / run markers (never commit these)
build-*.done
build-*.log
*.done
.lsp_pass_timestamp
testnet/genesis-weighted-full.json
devnet/wsl/genesis-3val.json

.torus/

# runtime / bench artifacts (kept on disk, ignored in git)
/data/
/data.*/
/node.log
/node.log.*
/node.pid
*.pid
testnet/p3-testnet/__pycache__/
testnet/p3-testnet/results/

# validator secrets — NEVER commit (seed.passphrase was public in git until 2026-07-21)
*.passphrase
*.keystore
